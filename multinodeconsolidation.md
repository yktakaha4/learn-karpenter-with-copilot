# MultiNodeConsolidation の ComputeCommand メソッド

## 目的
`ComputeCommand` メソッドは、複数のノードを同時に統合（Multi-Node Consolidation）するためのコマンドを生成することを目的としています。このメソッドは、候補ノード（Candidates）を評価し、リソースの再スケジューリングが可能かどうかを判断します。最終的に、複数ノードの統合を効率的に実行するためのコマンドを生成します。

## 処理のロジック

### 1. Consolidation 状態の確認
- `m.IsConsolidated()` を呼び出して、すでに統合が完了しているかを確認します。
- 統合が完了している場合、空のコマンドを返して処理を終了します。

### 2. 候補ノードのソート
- `sortCandidates` メソッドを使用して、候補ノードを中断コスト（Disruption Cost）に基づいてソートします。

### 3. 中断予算の適用
- 各候補ノードについて、中断予算（Disruption Budget）を確認します。
  - 中断予算が許容されている場合、候補リストに追加します。
  - 中断予算が不足している場合、その候補をスキップします。

### 4. 最大バッチサイズの設定
- 最大100ノードのバッチサイズを設定します。
- バッチサイズは、計算コストを抑えるために制限されています。

### 5. 最適な統合コマンドの計算
- `firstNConsolidationOption` メソッドを使用して、最適な統合コマンドを計算します。
  - バイナリサーチを使用して、最適なノードセットを見つけます。
  - 各ノードセットについて、`computeConsolidation` メソッドを呼び出して統合可能性を評価します。

### 6. 統合コマンドの検証
- `NewValidation` を使用して、生成されたコマンドが有効であるかを検証します。
- コマンドが無効な場合、空のコマンドを返します。

## 最終的な出力結果
- 統合可能なコマンド（`Command`）とスケジューリング結果（`scheduling.Results`）を返します。
- 統合が不可能な場合、空のコマンドと結果を返します。

## 入力に対する出力のサンプル

### 入力
- 候補ノード：
  - Node A（中断コスト: 10, 再スケジュール可能なPod: 3）
  - Node B（中断コスト: 5, 再スケジュール可能なPod: 2）
  - Node C（中断コスト: 15, 再スケジュール可能なPod: 0）
- 中断予算：
  - NodePool A: 2
  - NodePool B: 1

### 出力
- 統合コマンド：
  - Node A と Node B を統合対象とするコマンド。
- スケジューリング結果：
  - Node A と Node B のPodが再スケジュールされるノード情報。

このメソッドは、複数ノードの統合を効率的に実行し、クラスタのリソース効率を向上させる重要な役割を果たします。
